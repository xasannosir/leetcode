SELECT s.product_id, s.year AS first_year, s.quantity, s.price FROM Sales AS s RIGHT JOIN (SELECT product_id, MIN(year) AS year FROM Sales GROUP BY product_id) AS first ON first.product_id = s.product_id AND first.year = s.year;
